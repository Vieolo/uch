# See https://goreleaser.com for documentation.
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

project_name: uch

before:
  hooks:
    - go mod tidy

builds:
  - id: uch
    main: ./
    binary: uch
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # -s -w strip debug info to keep the binary small.
    # Version is read from the embedded go.yaml at run time, so no -X needed.
    ldflags:
      - -s -w

archives:
  - id: uch
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - README.md
      - changelog.md

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"
      - "Merge branch"

brews:
  - name: uch
    repository:
      owner: vieolo
      name: homebrew-tap
      branch: master
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/vieolo/uch"
    description: "Universal Command Helper — store, encrypt, and run your re-used terminal commands by nickname."
    license: "MIT"
    # Skip the brew push for snapshots and prereleases (e.g. v1.2.3-rc1).
    skip_upload: auto
    install: |
      bin.install "uch"
    test: |
      system "#{bin}/uch", "version"

release:
  draft: false
  prerelease: auto
  footer: |
    ---
    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
